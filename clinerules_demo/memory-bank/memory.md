# Memory Bank

This directory has been initialized as the memory bank for the project.

You can use this folder for configuration, persistent data storage, or files related to memory-based features.

## Project State (as of latest update)

- Automated Selenium + TestNG tests have been created under src/test/java/com/example/UserLoginTest.java.
- Tests cover both 'standard_user' and 'problem_user' login workflows on https://www.saucedemo.com.
- Screenshots are captured post-login for each user and saved to the local Pictures/Screenshots directory.
- Project uses Java 17, manages dependencies with Maven, and supports concurrent test execution via a generated testng.xml suite.
- JavaScript calculator module created at js/Calculator.js for add operations.
- index.html web app created for user-driven addition of two numbers (input, button, result).
- Use this memory bank to track additional features, store logs, or persist memory-related artifacts as the project evolves.
